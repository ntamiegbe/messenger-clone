'use client'

import clsx from "clsx"
import Link from "next/link"

type MobileFooterItemProps = {
    label: string;
    icon: any;
    href: string;
    active?: boolean;
}

const MobileFooterItem = (props: MobileFooterItemProps) => {
  return (
    <div>MobileFooterItem</div>
  )
}

export default MobileFooterItem